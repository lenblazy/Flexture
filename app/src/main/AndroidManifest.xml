<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lennox.flexture">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Registration" />
        <activity
            android:name=".Flexture"
            android:theme="@style/myStyle" />
        <activity
            android:name=".Profile"
            android:parentActivityName=".Flexture"
            android:theme="@style/myStyle" />
        <activity
            android:name=".About"
            android:parentActivityName=".Flexture"
            android:theme="@style/myStyle" />
        <activity
            android:name=".ClassSession"
            android:label="Class Session"
            android:parentActivityName=".Flexture"
            android:theme="@style/myStyle" />
        <activity
            android:name=".Scheduler"
            android:parentActivityName=".Flexture"
            android:theme="@style/myStyle" />
        <activity
            android:name=".Notes"
            android:parentActivityName=".Flexture"
            android:theme="@style/myStyle" />
        <activity android:name=".Results"
            android:parentActivityName=".Flexture"
            android:theme="@style/myStyle"></activity>
    </application>

</manifest>